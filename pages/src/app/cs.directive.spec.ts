import { CsDirective } from './cs.directive';

describe('CsDirective', () => {
  it('should create an instance', () => {
    const directive = new CsDirective();
    expect(directive).toBeTruthy();
  });
});
